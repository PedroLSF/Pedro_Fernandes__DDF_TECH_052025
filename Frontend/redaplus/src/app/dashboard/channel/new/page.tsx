import { ChannelCreateView } from 'src/sections/channel/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Criar Canal',
};

export default function ChannelCreatePage() {
  return <ChannelCreateView />;
}

import { ChannelListView } from 'src/sections/channel/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Listar Canais',
};

export default function ChannelListPage() {
  return <ChannelListView />;
}

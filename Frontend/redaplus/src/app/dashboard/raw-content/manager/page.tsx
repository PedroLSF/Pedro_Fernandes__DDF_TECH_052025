import { FileManagerView } from 'src/sections/raw-content-manager/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Gerenciador',
};

export default function RawContentManagerPage() {
  return <FileManagerView />;
}

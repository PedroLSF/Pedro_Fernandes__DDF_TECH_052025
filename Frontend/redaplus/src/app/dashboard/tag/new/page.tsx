import { TagCreateView } from 'src/sections/tag/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Criar Tags',
};

export default function TagCreatePage() {
  return <TagCreateView />;
}
